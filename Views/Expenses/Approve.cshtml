@model HRMS.Models.Expenses
@{
    ViewData["Title"] = "Approve Expense";
}

<div class="container mt-5">
    <div class="card shadow-lg border-0 rounded-3">
        <div class="card-header bg-success text-white text-center">
            <h4>Approve Expense</h4>
        </div>
        <div class="card-body">
            <h5 class="text-secondary">Are you sure you want to approve this expense?</h5>
            <hr />
            <dl class="row">
                <dt class="col-sm-3">Expense Type</dt>
                <dd class="col-sm-9">@Model.ExpenseType</dd>

                <dt class="col-sm-3">Amount</dt>
                <dd class="col-sm-9">@Model.Amount</dd>

                <dt class="col-sm-3">Description</dt>
                <dd class="col-sm-9">@Model.Description</dd>
            </dl>

            <form asp-action="Approve" method="post">
                <input type="hidden" asp-for="Id" />
                <button type="submit" class="btn btn-success">✅ Approve</button>
                <a asp-action="Index" class="btn btn-secondary">Cancel</a>
            </form>
        </div>
    </div>
</div>
